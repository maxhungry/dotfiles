-- Don't display 'help' on startup
\set QUIET 1

-- Promt
\set PROMPT1 '%[%033[1m%]%M %n@%/%R%[%033[0m%]%# '
\set PROMPT2 '[more] %R > '

-- Show time for queries
\timing

-- Format
\x auto

-- Show nulls
\pset null '(NULL)'
